---
"@itwin/presentation-tree-definitions": patch
---

Added `createClassificationsTree` to create configurable classification hierarchies with bound search helpers for labels and instance keys. Added `ClassificationsTreeNode` utilities for identifying and narrowing classifications tree nodes.
